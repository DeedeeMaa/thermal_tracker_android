# 迷糊热成像 V1.0 版 —— 开发历程与踩坑警示录

> 把 Python PC 版热成像热点追踪软件移植为安卓 App 的全过程记录。
> **本文档是"血的教训"，改代码前先读一遍，避免重蹈覆辙。**

---

## 一、项目概况

| 项 | 值 |
|---|---|
| 相机 | 海康威视 DS-2TA03-15SVI 热成像 |
| 相机 IP | `192.168.2.104`（掩码 255.255.255.0，网关 192.168.2.1） |
| 账密 | `admin` / `asd37210` |
| 端口 | RTSP 554 / SDK 8000 / HTTP 80（都通） |
| 安卓技术栈 | Kotlin + Jetpack Compose + OpenCV 4.9.0 + Media3 ExoPlayer(RTSP) + 海康 HCNetSDK(JNA) |
| 包名 | com.thermal.tracker |
| 版本 | 迷糊热成像V1.0版（黑猫图标） |

---

## 二、核心踩坑记录（按时间顺序）

### 1. 相机连不上 / 网络不通 ⚠️

**现象**：手机通过路由器/网桥/中继后，相机经常失联。

**结论**：
- 相机的 WiFi 中继要设为 **Repeater（无线客户端桥接）**模式，不是 AP 模式。
- 主路由器固定 `192.168.2.1`，相机固定 `192.168.2.104`，手机连同一路由器 WiFi。
- 排查顺序：先 Ping 通 → 再测 554 端口 → 再测 8000 端口。

### 2. 黑屏无画面（最隐蔽的坑）⚠️⚠️

**现象**：RTSP 握手成功但永远黑屏。

**根因**：`SurfaceTexture.onFrameAvailable` 回调被发到 HandlerThread 的 Looper，
而处理循环用了 `while + sleep` **死循环**，把 Looper 饿死了 → 回调永远不执行 → 抓不到帧。

**修复**：**绝不用 while 死循环阻塞 HandlerThread**。改成 `handler.post(::processStep)` 链式调度，
无新帧时 `postDelayed(10ms)` 重试。抓帧用 try/catch 包住 `updateTexImage()`。

**警示**：凡是"回调不触发"的问题，先怀疑是不是 HandlerThread 被自己阻塞了。

### 3. 按钮卡顿 / 延迟大 ⚠️

**现象**：截图等按钮点了半天没反应。

**根因**：整个 LiveScreen 每帧重组——预览、统计、轨迹全部高频 `collectAsState`，
主线程被拖垮。

**修复**：
- 预览做成**独立 Composable**（只有它收集 preview）。
- 预览限流 ≥33ms、统计/轨迹限流 ≥200ms。

### 4. RTSP 连不上（ExoPlayer + 海康）⚠️⚠️

**现象**：自定义 LoadControl 缓冲、自定义 User-Agent 都会导致海康相机 RTSP 握手失败。

**结论**：**海康相机用 ExoPlayer 必须保持默认 LoadControl 和默认 User-Agent**。
任何"优化"都是坑。教训：不要乱改这两个。

### 5. 温度显示 "??C" ⚠️

**根因**：OpenCV `putText` 的 Hershey 字体**不支持 `°`/`℃` 字符**，渲染成 `?`。

**修复**：只用 ASCII，如 `23.5C`。

### 6. 手动调焦电机不动（本工程最曲折的坑）⚠️⚠️⚠️

**现象**：安卓上按住"调近/调远"电机不动，但 PC 版能调。

**排查链条**（别跳过，每一步都是坑）：
1. **相机在自动对焦模式下会忽略 PTZ 调焦命令** → 调焦前必须先 `setFocusMode(1)` 切手动
   （`NET_DVR_GetDVRConfig(3305)` 读 → 改 `byFocusMode=1` → `NET_DVR_SetDVRConfig(3306)` 写）。
2. **JNA 封装缺方法**：安卓 SDK 的 `HCNetSDKByJNA.java` 原本**没有**
   `NET_DVR_PTZControl_Other` 和 `NET_DVR_Login_V30`，要自己补上
   （注意该文件是 **Tab 缩进**，用空格匹配会失败）。
3. **停止电机必须用 `NET_DVR_PTZControl_Other(userId, ch, cmd, stop=1)`**（PC 验证过的写法），
   用 `PTZControlWithSpeed_Other(stop=1, speed)` 停止对这台相机无效。
4. **登录失败会静默回退 Mock 模拟 SDK** → 聚焦命令全打在假 SDK 上，电机当然不动！
   UI 必须显示真实/模拟状态，否则根本看不出。
5. **位置寄存器范围不是 0~4000**：实测 `dwRelativeFocusPos & 0xFFFF` 可到 36213，
   PC 注释"0~4000"是错的，**不要除以 4000**。`dwFocusPos`（绝对位置）恒为 0 无用。
6. **按住手势用 `detectTapGestures` 会一按就释放**，电机只转一瞬。
   要用 `awaitEachGesture + awaitFirstDown`，且按住期间**每 250ms 重发一次 start**。
7. **ISAPI HTTP 调焦不支持**：`PUT /ISAPI/PTZCtrl/channels/1/focus` 返回 403 `notSupport`，
   这台相机只能走 SDK 方式调焦。

**最终结论**：登录成功 ≠ 命令有效 ≠ 电机转动，必须逐层打日志/显示验证。

---

## 三、海康安卓 SDK 集成要点

- 库文件：
  - `app/libs/`：`HCNetSDK.jar`、`HCNetSDK_E.jar`、`jna.jar`、`PlayerSDK_hcnetsdk.jar`
  - `app/src/main/jniLibs/<abi>/`：libhcnetsdk.so 及配套 so
    （arm64-v8a 20 个，armeabi-v7a 23 个；arm64 本来就没有 libhpr/libopensslwrap，正常）
- **`HCNetSDK.jar` 只含 JNI 封装（com.hikvision.netsdk）**；
  **JNA 封装（com.hcnetsdk.jna.HCNetSDKByJNA）是 Demo 源码**，要拷进工程一起编译。
- 登录：`NET_DVR_Login_V40` 失败回退 `NET_DVR_Login_V30`（PC 用 V30 成功）。
- 调焦命令：`NET_DVR_PTZControlWithSpeed_Other(userId, ch, 13/14, stop=0, speed=2)` 启动；
  `NET_DVR_PTZControl_Other(userId, ch, 13/14, stop=1)` 停止。
- 全屏测温：`NET_DVR_CaptureJPEGPicture_WithAppendData` → 解析 P2P 数据
  （小端 float32，行优先，width×height 个 ℃）。

---

## 四、V1.0 功能清单

| 功能 | 状态 |
|---|---|
| RTSP 实时画面（ExoPlayer，TCP/UDP） | ✅ |
| 热点检测 + 追踪 + 伪彩渲染 | ✅ |
| 调色板（ironbow/白热/inferno/jet） | ✅ |
| 手动调焦（按住调近/调远） | ✅（切手动模式 + 250ms 重发） |
| 真实测温（海康 SDK 全屏矩阵） | ✅（登录失败回退模拟） |
| **手动测温框**（拖拽画框，≤20 个，显示均/峰/低温） | ✅ V1.0 新增 |
| **温度线**（拖拽画线，≤4 条，线上逐点温度） | ✅ V1.0 新增 |
| 截图（系统屏幕截图 MediaProjection） | ✅ V1.0 改为系统录屏 |
| 录像（系统录屏 MediaProjection） | ✅ V1.0 改为系统录屏 |
| CSV 导出（热区表/全矩阵） | ✅（周期滑条已暂时移除） |
| 黑猫图标 + "迷糊热成像V1.0版" | ✅ |

---

## 五、屏幕截图/录像（MediaProjection）注意事项

- 必须：`FOREGROUND_SERVICE` + `FOREGROUND_SERVICE_MEDIA_PROJECTION` 权限，
  Manifest 里 service 声明 `android:foregroundServiceType="mediaProjection"`。
- 流程：点按钮 → 无投影则 `createScreenCaptureIntent()` 系统授权 →
  拿到 `MediaProjection` 存到 `ScreenCaptureService.projection` →
  **再** `startForegroundService`（Android 14+ 必须先有授权再启服务，5 秒内）。
- 截图：`ImageReader` + `VirtualDisplay` 抓一帧存 PNG；录像：`MediaRecorder` + `VirtualDisplay` 录 MP4。
- 文件输出到 `<外部私有目录>/screenshots/` 和 `/recordings/`。

---

## 六、遗留 / 待办

- 测温框/温度线统计目前每 200ms 刷新（跟统计节流一起），数量多时注意性能。
- CSV 周期滑条暂时移除，如需要可从 `cfg.csv.samplePeriodMs` 恢复。
- 海康 JNA 封装的 `NET_DVR_RealPlay_V40` 被官方 Demo 注释掉了；若以后要预览句柄功能需自行补上。

---

*写于 2026-08-09。改代码前请先看第二节「核心踩坑记录」。*
